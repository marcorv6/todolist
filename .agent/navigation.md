# Todolist Navigation Map

- `app/`: Next.js App Router pages and API endpoints.
- `components/`: UI components (task items, filter tabs, modal dialogs).
- `context/`: State management providers.
- `lib/`: Database helpers (`lib/db/`) and utility functions.
